apiVersion: apps/v1
kind: Deployment
metadata:
  name: matas-apache-deployment
spec:
  replicas: 2
  template:
    spec:
      containers:
      - name: matas-apache-container
        resources:
          requests:
            cpu: "80m"
